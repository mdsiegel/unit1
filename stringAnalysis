#Matthew Siegel
#9/1/17
#stringAnalysis.py - Analysing a string

sentence1 = input('Enter a sentence: ')
sentence = sentence1.lower()
words = len(sentence.split())
spaces = sentence.count(' ')
characterCount = len(sentence)
letterCount = characterCount - spaces

letterSearch = input('Enter a character to search for:')

wordSearch = input('Enter a word to search for:')
print('Your sentence has', sentence.count(letterSearch), 'cases of the letter', letterSearch)

print('There are',words, 'words and', characterCount, 'characters and',letterCount, 'letters.' )

print('Your sentence has', sentence.count(wordSearch), 'cases of the word', wordSearch)
